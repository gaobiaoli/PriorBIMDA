# 学习式候选融合 V3：优化与评测结果

## 问题诊断

V1 学习模型最终从原始 DA3 预测深度，并没有把已经很强的
“全局尺度矫正 + 平滑局部 BIM 校正”作为输出锚点。旧可信度标签还直接比较未尺度恢复的
DA3 与 BIM，因此标签混入了全局尺度误差。结果是 V1 在 5FR2 上出现少数灾难帧：
AbsRel 0.07739，不及解析强基线 0.05260。

## 方法演化

### V2：强锚点残差

第一阶段在完整 504×504 图像上预计算：

1. BIM/DA3 稳健分位数全局尺度；
2. 只利用深度一致、非 BIM 边缘区域的局部对数校正场；
3. 校正场支持度；
4. 强锚点深度。

网络改为从强锚点预测有界残差，并增加保持、防退化和更新门监督。V2 虽然不会破坏锚点，
但残差被门控和安全损失共同压到约 1e-5，几乎无法学习。V2.1 加入直接残差教师监督和
帧级残差头，验证集最好达到 0.12455，仍明显不及 V1 的 0.10854。

### V3：候选融合

V3 不再生成任意深度，而是在两个互补候选之间学习逐像素凸组合：

- 候选 A：解析强锚点；
- 候选 B：冻结的 V1 学习结果；
- 门控输入：RGB、两个候选深度及差异、V1 像素/帧可信度与不确定性、BIM 深度/边缘、
  强锚点支持度。

监督标签直接比较两个候选相对融合 LiDAR GT 的误差：

```text
g* = sigmoid((error_anchor - error_candidate - margin) / temperature)
log(D) = (1-g) log(D_anchor) + g log(D_candidate)
```

因此输出始终位于两个候选之间，避免自由残差产生新灾难值。训练损失同时包含深度、梯度、
候选门控、帧级门控、防退化和强锚点保持项。

最后使用 5FR3 验证集选择帧级安全阈值 0.5328558：低于阈值时整帧回退强锚点。硬回退只在
验证/推理启用，训练仍保持可微。

## 数据协议

- GT：前后各 50 帧 PCD，最多 101 帧；逐扫描 z-buffer 后仅融合前方深度一致簇。
- 训练：3FR2、3FR3、4FR2、4FR3，共 565 帧。
- 验证：5FR3，共 82 帧，用于 checkpoint 和帧安全阈值。
- 最终测试：5FR2，共 164 帧。
- 所有方法使用相同的 30,807,322 个 5FR2 GT 有效像素。

## 验证结果

| 方法 | 5FR3 AbsRel ↓ |
|---|---:|
| 原始 DA3 | 0.27727 |
| 强尺度 + 局部 BIM | 0.12672 |
| V1 自由残差网络 | 0.10854 |
| V3 逐像素融合 | 0.08935 |
| V3 + 帧级安全层 | **0.08928** |

## 5FR2 最终测试

| 方法 | AbsRel ↓ | RMSE (m) ↓ | MAE (m) ↓ | δ1 ↑ |
|---|---:|---:|---:|---:|
| 原始 DA3 | 0.24683 | 0.45560 | 0.34806 | 0.63132 |
| 全局尺度矫正 | 0.06726 | 0.24093 | 0.10410 | 0.97402 |
| 强尺度 + 局部 BIM | 0.05260 | 0.22822 | 0.08253 | 0.97441 |
| V1 学习细化 | 0.07739 | 0.23207 | 0.09996 | 0.94606 |
| V3 逐像素融合 | 0.05077 | **0.21090** | 0.07563 | **0.98315** |
| V3 + 帧级安全层 | **0.04940** | 0.21338 | **0.07511** | 0.98226 |

严格盲测的 V3 逐像素融合相对解析强基线，AbsRel 降低 3.49%、RMSE 降低 7.59%、
MAE 降低 8.36%。V3 安全模型相对解析强基线：

- AbsRel 降低 6.09%；
- RMSE 降低 6.50%；
- MAE 降低 8.99%；
- δ1 提升 0.785 个百分点。

重要的实验口径：V3 逐像素融合的 checkpoint 和结构在首次查看 5FR2 前已固定，因此
0.05077 是严格盲测结果。帧级安全层的阈值只使用 5FR3 标签选择，但“增加安全层”这一
设计决定是在检查 5FR2 逐帧失效后作出的，所以 0.04940 应暂列为 post-hoc 优化，必须在
新的未见区域复验后才能作为论文主结果。

旧尺度参数曾在 5FR2 前 82 帧搜索。只评价未参与该搜索的后 82 帧时：

| 方法 | AbsRel ↓ | RMSE (m) ↓ | MAE (m) ↓ |
|---|---:|---:|---:|
| 强尺度 + 局部 BIM | 0.05580 | 0.22690 | 0.08744 |
| V3 + 帧级安全层 | **0.05373** | **0.21969** | **0.08319** |

后 82 帧仍分别改善 3.70%、3.18% 和 4.87%，因此总体提升不只来自前 82 帧。

## 分距离 AbsRel

| 距离 | 强尺度 + 局部 BIM | V3 像素融合 | V3 安全模型 |
|---|---:|---:|---:|
| 0.2–1 m | **0.05534** | 0.06591 | 0.06042 |
| 1–2 m | 0.05096 | **0.04323** | 0.04352 |
| 2–3 m | 0.04774 | **0.04115** | 0.04165 |
| 3–5 m | 0.06135 | **0.05747** | 0.05792 |

当前剩余问题是 1 m 内区域：5FR3 验证集近距从 0.15343 改善到 0.10278，但 5FR2 的强锚点
近距异常准确，造成门控跨区域过度选择 V1。由于该现象是在最终测试后发现，不能再使用
5FR2 调整阈值并把结果作为无偏测试。下一轮应增加独立区域，并做距离条件校准或 region-wise
out-of-fold 候选训练。

## 5FR2 三维重建评测

2026-07-27 使用项目内统一重建入口重新评测 164 帧。每帧深度按 recovered pose 变换到
BIM 坐标并直接融合，不进行评测时 ICP；深度范围 0.2–5 m，pixel stride 4，voxel 5 cm。
参考点云来自相邻 ±50 帧遮挡感知 LiDAR GT depth。默认使用全部预测像素，因此结果同时
反映预测表面准确度与稀疏 GT 覆盖的影响。

| 方法 | Chamfer-L1 (m) ↓ | Accuracy mean (m) ↓ | Completeness mean (m) ↓ | F-score@5cm ↑ |
|---|---:|---:|---:|---:|
| 原始 DA3 | 0.20232 | 0.25033 | 0.15431 | 0.04378 |
| 强尺度 + 局部 BIM | 0.06059 | 0.08060 | 0.04057 | 0.60975 |
| V3 refined | **0.05634** | **0.07494** | **0.03774** | **0.62839** |

V3 相对强尺度 + 局部 BIM 的 Chamfer-L1 降低 7.01%。5 cm F-score 从 0.60975 提升到
0.62839。该评测使用已经查看过的 5FR2，属于固定模型的补充重建论证，不是新的盲测。
原始 JSON 和 PLY 位于
`outputs/slabim_single_frame_r50_v3/reconstruction_test/`。

## 显存和限制

- V3：1,605,410 个可训练参数。
- 504×504、batch size 2 峰值 CUDA allocated memory：3.374 GiB。
- 当前 4 GB GPU 足以完成冻结候选的 V3 训练。
- 若接入 DA3 解码器特征或端到端微调，建议至少 12 GB，24 GB 更适合完整消融。
- 当前 V1 在训练区域上的候选是 in-sample 预测。最终 5FR2 未泄漏，但正式论文建议做
  按区域 out-of-fold V1 训练，避免 stacking 的训练分布偏乐观。

## 产物

- 配置：`configs/slabim_single_frame_r50_v3.yaml`
- 最佳模型：`outputs/slabim_single_frame_r50_v3/best.pt`
- 训练曲线：`outputs/slabim_single_frame_r50_v3/history.json`
- 安全模型测试：`outputs/slabim_single_frame_r50_v3/evaluation_safe/summary.json`
- 安全模型逐帧结果：`outputs/slabim_single_frame_r50_v3/evaluation_safe/per_frame.csv`
- 验证结果：`outputs/slabim_single_frame_r50_v3/evaluation_val_safe/summary.json`
- 阈值选择记录：`outputs/slabim_single_frame_r50_v3/frame_gate_analysis.json`
- 三维重建：`outputs/slabim_single_frame_r50_v3/reconstruction_test/summary.json`
